import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blaze.booking',
  appName: 'blaze-booking',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
